# BillLens uses Firebase/Firestore — no local database containers needed.
# This file is kept for the Firebase Emulator Suite if you want local emulation.
#
# To use Firebase emulators instead:
#   npx firebase emulators:start
#
# See https://firebase.google.com/docs/emulator-suite for details.
